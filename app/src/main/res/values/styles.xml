<resources>

    <style name="AppTheme" parent="AppTheme.Base"/>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cian</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/cian_dark</item>
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/red</item>
    </style>

    <!-- ActionBar styles -->
    <style name="TransparentActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:displayOptions">showHome|useLogo</item>
        <item name="android:titleTextStyle">@style/TransparentActionBar.TitleTextStyle</item>
    </style>

    <style name="TransparentActionBar.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="DetailsTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
    </style>

</resources>
